{
  "name": "tabris-remote-app",
  "version": "0.2.0",
  "main": "dist/app.js",
  "private": true,
  "scripts": {
    "test": "tslint 'src/**/*.ts'",
    "prepare": "node scripts/npm-login.js",
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "start": "npm-run-all --parallel watch serve",
    "serve": "tabris serve"
  },
  "dependencies": {
    "tabris-js-remote": "3.4.0-beta1-nightly.93",
    "tabris": "^2.4.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.0.2",
    "tslint": "^5.3.2",
    "typescript": "^2.3.3"
  }
}
