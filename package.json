{
  "name": "tabris-remote-app",
  "version": "0.1.0",
  "main": "dist/app.js",
  "private": true,
  "scripts": {
    "test": "tslint 'src/**/*.ts'",
    "prepare": "node scripts/npm-login.js",
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "start": "npm-run-all --parallel watch serve",
    "serve": "tabris serve"
  },
  "dependencies": {
    "tabris-js-remote": ">=0.1.0-nightly.0 <0.1.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.0.2",
    "tslint": "^5.3.2",
    "typescript": "^2.3.3"
  }
}
